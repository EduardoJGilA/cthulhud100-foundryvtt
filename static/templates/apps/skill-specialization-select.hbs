<div class="flexcol" style="gap: 0.375rem;">
  {{#if allowSelect}}
    <div class="flexrow">
      <select name="existing-skill">
        <option value="">{{localize 'Cd100.SkillPickNameOnly'}}</option>
        {{selectOptions skills selected=selected valueAttr='id' labelAttr='name'}}
      </select>
    </div>
  {{/if}}
  {{#if (and allowCustom (eq selected ''))}}
    <div>
      <div class="form-group">
        <label>{{localize 'Cd100.CustomSpecialisationLabel' specialisation=label}}</label>
      </div>
      <div class="form-group">
        <input name="new-skill-name" type="text" value="{{name}}" placeholder="{{localize 'Cd100.CreateNewSkill'}}" />
      </div>
    </div>

    {{#unless fixedBaseValue}}
      <div class="form-group">
        <label>{{localize 'Cd100.BaseSkillValue'}}</label>
        <div class="form-fields">
          <input name="base-value" type="text" value="{{baseValue}}" placeholder="{{localize 'Cd100.SkillBase'}}" />
        </div>
      </div>
    {{/unless}}
  {{/if}}
</div>